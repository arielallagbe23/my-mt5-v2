PORT=4000
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=replace-with-a-long-random-string
GMAIL_USER=ton-adresse@gmail.com
GMAIL_APP_PASSWORD=replace-with-a-16-char-app-password

# Local dev only: path to the service account JSON file.
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Production (Vercel) only: full service account JSON as a single-line string.
# Leave unset locally — GOOGLE_APPLICATION_CREDENTIALS above is used instead.
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
